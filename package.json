{
  "type": "module",
  "scripts": {
    "dev": "tsc --watch -p tsconfig.dev.json",
    "minify": "node minify.js",
    "build": "tsc -p tsconfig.prod.json && node minify"
  },
  "devDependencies": {
    "terser": "^5.19.1",
    "typescript": "^5.1.6"
  }
}
